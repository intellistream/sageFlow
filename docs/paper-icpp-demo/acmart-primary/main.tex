\documentclass[sigconf]{acmart}

\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

\setcopyright{none}
\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{}

\usepackage{booktabs}
\usepackage{placeins}
\usepackage{tikz}

\usetikzlibrary{arrows.meta,positioning}

\newcommand{\system}{\textsc{BriskSnapshot}}
\setlength{\textfloatsep}{8pt}
\setlength{\floatsep}{7pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{2pt}

\begin{document}

\title{\system{}: A Live Demo of Join-Backed Semantic Windows for Streaming AI Pipelines}

\author{Anonymous Author(s)}
\acmConference[ICPP '26]{The 55th International Conference on Parallel Processing}{September 28--October 1, 2026}{Singapore}
\acmYear{2026}
\copyrightyear{2026}

\begin{abstract}
Streaming AI applications need to keep vector state fresh while exposing that state in a form that downstream services can inspect and act on. \system{} is a live demonstration of a unified semantic-window system implemented over a streaming AI control layer and a vector-stream runtime~\cite{sage_icml2026}. The demo follows public vulnerability-intelligence reports as they are normalized, embedded with BGE, joined inside a bounded semantic window, and surfaced as snapshot contracts for a provider-agnostic generator service. Unlike a static retrieval demo, \system{} lets the audience configure the stream, join strategy, retention window, parallelism, and generator endpoint while seeing how those choices affect runtime evidence and downstream answers. The live walkthrough stays small enough for narration, while a 3,000-record NVD replay and a 20-answer generator run show measured behavior under real embeddings and API-backed generation.
\end{abstract}

\ccsdesc[500]{Computer systems organization~Parallel architectures}
\ccsdesc[500]{Information systems~Stream management}
\ccsdesc[300]{Computing methodologies~Artificial intelligence}

\keywords{stream processing, vector search, AI middleware, semantic windows, demonstration}

\maketitle

\section{Introduction}

Large-model applications increasingly run as long-lived dataflows rather than as isolated prompt calls. A vulnerability assistant receives new advisories while an investigation is still open; a retrieval pipeline refreshes its vector neighborhood between two user questions; an on-call agent must decide whether a new report belongs to a known incident or deserves escalation. In these settings, the model-facing component should not receive an opaque vector index or a free-form prompt dump. It needs bounded, inspectable evidence: which records are retained, which records matched, which sources support the cluster, and which action the system recommends.

Stream processors provide scalable dataflow execution~\cite{akidau2015dataflow,carbone2015apache}, while vector indexes provide efficient approximate search~\cite{jegou2011product,johnson2019billion,malkov2020efficient}. Retrieval-augmented systems further show how language models benefit from external evidence~\cite{lewis2020rag,guu2020realm}. What is still awkward in many prototypes is the boundary between streaming state and AI orchestration. Semantic state is often rebuilt outside the component that observes the stream, so a demo can show an answer but not the live runtime evidence that made the answer possible.

\system{} demonstrates a narrower and more observable boundary. The system owns the active semantic window, runs vector joins as events arrive, and exports only compact contracts to the application layer. A contract is not another prompt; it is a structured object with cluster identity, supporting neighbors, source coverage, novelty, route, priority, and action fields. Those fields are then rendered in the UI and placed into the generator context, so the audience can trace an answer back to runtime-owned evidence rather than to hidden prompt construction. The demo is built around the question an audience naturally asks during a live system demonstration: when the next event arrives, what changes, why did it change, and what can the upper pipeline safely do with the change?

The paper keeps the original demo narrative but makes three pieces explicit. First, it describes the system architecture as a single integrated prototype rather than two adjacent components. Second, it makes the live console itself part of the contribution: attendees can zoom from the end-to-end application pipeline into the semantic-window operator path, inspect aggregated evidence, and verify the contract consumed by the generator. Third, it adds prepared evidence from real public records and real embedding vectors, including a parallelism sweep and a generator run that show the capability of the prototype without turning the paper into a full benchmark paper.

\section{System Overview}

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.82\textwidth]{figures/brisk_architecture.png}
  \caption{\system{} architecture. Experiment controls configure the stream, runtime, and generator; the semantic-window runtime owns retained vector state, joins, pair emission, cluster state, and snapshot export; the contract/generation layer consumes bounded evidence for traceable answers.}
  \Description{Architecture diagram showing experiment controls above an input and orchestration layer, a stateful semantic-window runtime, and a contract and generation layer, with live console observability below.}
  \label{fig:overview}
\end{figure*}

Figure~\ref{fig:overview} shows the implemented path. \system{} takes heterogeneous vulnerability reports from multiple sources, maps each report into a 384-dimensional \texttt{BAAI/bge-small-en-v1.5} embedding, appends the record into a bounded semantic window, and drains join pairs produced by the persistent runtime. The output is then folded into application-level contracts and, in the API-backed mode, into a bounded prompt context for an OpenAI-compatible generator endpoint. This organization is intentionally close to the live demo: the top-level event-to-answer pipeline stays visible while the semantic-window stage expands into append lanes, window state, join strategy, pair emission, cluster update, and snapshot export. The audience can therefore follow the same event from stream input to runtime-owned evidence to generated answer.

\subsection{Design Goals}

The first design goal is \emph{observable freshness}. A static retrieval index can demonstrate relevance, but it hides whether the context is current. \system{} keeps the current window visible: retained records, emitted pairs, active clusters, and newest evidence are all displayed as runtime state rather than as post-hoc explanation text.

The second goal is \emph{bounded handoff}. Model-facing services should not receive an unbounded list of nearest neighbors. The system exports a small set of fields that can be logged, routed, or summarized: cluster identity, source coverage, supporting neighbor identifiers, novelty, route, priority, and action. This turns the semantic window into an application interface rather than a private runtime detail.

The third goal is \emph{demo reproducibility}. Live demos are convincing when they are interactive, but papers also need evidence that the observed behavior is not a hand-picked screen. The same replay driver used for the UI can run public NVD/CISA inputs, cached real embeddings, threshold variants, and window-size variants. The prepared figure in Section~\ref{sec:evidence} is generated from those replay artifacts.

\subsection{Runtime Boundary}

The runtime boundary is deliberately narrow. The ingestion layer gives the runtime comparable vectors and stable identifiers. The window manager owns which events are still active and which events have left the current view. The join runtime is the only component that creates semantic match pairs. The contract layer interprets those pairs for an application: a dense multi-source cluster becomes an incident snapshot, a cluster with a clear owner becomes a routing contract, and a high-severity novel event becomes an escalation contract. The UI exposes the same boundary through the pipeline view, runtime zoom, evidence tables, contract inspector, and generator metrics.

Cluster construction uses connected components over returned join pairs. Clusters are sorted by size and average severity before export, which makes the display stable enough for a presenter to narrate. The default incident rule requires at least three events, average severity above 0.70, and at least two source classes. The emerging-risk rule fires when a high-severity event remains novel enough to deserve attention even before a larger cluster forms.

\section{Live Demonstration}

The live scenario uses public vulnerability-intelligence records from NVD, vendor advisories, CISA warnings, emergency alerts, and research notes. The reports cover familiar incident families such as PAN-OS, XZ Utils, OpenSSH, PHP-CGI, Fortinet VPN, and TeamCity. This domain is useful for a systems demo because records arrive from different sources, use different language, and still need to be grouped quickly enough for operator-facing workflows. The console is therefore organized as an experiment surface: the presenter can choose replay source, join method, window size, threshold, parallelism, and generator backend before following the same configured run through pipeline state, runtime operators, evidence, and generated output. The larger evidence run uses 3,000 NVD records published in the first quarter of 2024, embedded once with BGE and replayed through the same runtime path.

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.98\textwidth,height=0.42\textheight,keepaspectratio]{figures/brisk_ui_walkthrough.png}
  \caption{Real console walkthrough. Annotated screenshots show the configurable pipeline console, semantic-window operator zoom, evidence workspace, contract-to-generation handoff, and metrics/profile view used during the live demonstration.}
  \Description{Annotated real screenshots of the BriskSnapshot web interface with experiment controls, an end-to-end pipeline, a semantic-window runtime zoom view, evidence tables, a contract inspector, a generator output panel, and runtime metrics.}
  \label{fig:ui}
\end{figure*}

\subsection{Audience Path}

Figure~\ref{fig:ui} shows the UI used in the demonstration. Its role is not to be a dashboard after the fact, but to make the experiment protocol visible while the pipeline runs. The top surface fixes the workload and runtime parameters. The pipeline view keeps the event-to-answer path in one place. The zoom view opens the semantic-window stage into the operators that actually create evidence: append lanes, retained state, join probe, pair stream, cluster update, and snapshot export. The lower surfaces then carry that evidence into rows, clusters, neighbor records, a bounded contract, and provider-agnostic generation.

The presenter can run the stream stepwise or as a replay, change configuration, and then inspect how the same pipeline responds. This is the main demonstration point: parameter changes are not frontend decoration. They alter the boundary between retained runtime state, emitted semantic matches, exported contracts, and the text produced by the generator. The audience therefore sees a traceable AI pipeline rather than a static retrieval example: each answer can be followed back through contract fields, cluster evidence, join output, and the configured semantic window.

The walkthrough is designed to answer five questions in order. What just arrived? Where is it in the pipeline? What did the runtime retain and join? Which records became evidence? What did the generator receive and produce? This is the same structure used in many systems demonstrations: start with a concrete user path, reveal internal state at the moment it matters, and close by showing measured evidence beyond the exact path shown on screen.

\subsection{Demonstration Scenarios}

The snapshot scenario shows why the bounded window matters. When related reports arrive from multiple sources, the system connects them into a single family and exports a compact contract with source coverage and neighbor identifiers. The UI lets the audience see that the generated summary is not a generic explanation; it is grounded in event identifiers, nearest-neighbor rows, and emitted join pairs.

The runtime-inspection scenario shows how the same event is visible at two levels. At the outer level, the application pipeline shows the control path from event to generator. At the inner level, the runtime view shows the operator path that owns the semantic state. This zoomed view is important for a demo paper because the system contribution is not only the final answer but the observable runtime boundary that produces the contract.

The generator scenario closes the loop. The audience can inspect the contract first, then compare it with the generated response and token/latency metadata. A provider-agnostic display keeps this stage reusable for local vLLM or API-backed services while preserving the main claim: the generator consumes bounded evidence, and evidence identifiers can be traced back to the runtime state.

\section{Prepared Evidence}
\label{sec:evidence}

The narrated walkthrough is intentionally small enough for a live audience. To show more than a toy sample, the prepared evidence uses 3,000 public NVD records from 2024-Q1. Embeddings are produced by \texttt{BAAI/bge-small-en-v1.5} through SentenceTransformers, normalized to 384 dimensions, sharded for artifact storage, and replayed from cache so the runtime experiment is deterministic. A sampled vector was recomputed from the original text and matched the cache within floating-point roundoff, which guards against reporting placeholder vectors.

\begin{figure}[t]
  \centering
  \input{generated/prepared_run_result_figure.tex}
  \caption{Prepared evidence from real runs. Top: clustered runtime replay on 3,000 NVD records with BGE embeddings; p=2 gives the best measured throughput in this configuration. Bottom: a 20-event contract-to-answer generator run reports answer coverage, fallback count, evidence faithfulness, and LLM latency.}
  \Description{A compact result table showing throughput, speedup, latency, and emitted pairs for clustered join over two window sizes and four parallelism settings, plus a generator run summary.}
  \label{fig:results}
\end{figure}

\subsection{Workloads and Metrics}

The runtime workload contains 3,000 NVD records and 6,000 vector insertions because each event is replayed into the two input sides of the join. We report feed-and-drain latency, event throughput, speedup relative to one worker at the same window size, and emitted pair count. The experiment uses \texttt{clustered\_join}, a low similarity threshold of 0.5, multicast fanout two, and sequence timestamps so window sizes correspond to recent vector arrivals rather than calendar time. The generator workload replays 20 public vulnerability events through the same contract layer and reports generated answers, fallback count, evidence faithfulness, and LLM p50/p95 latency separately from runtime throughput.

Figure~\ref{fig:results} shows the main result. With a 512-arrival window, throughput improves from 22.7k events/s at p=1 to 26.4k events/s at p=2. With a 1,024-arrival window, throughput improves from 18.0k events/s to 28.1k events/s, a 1.57$\times$ speedup. Across both windows, the p=2 clustered profile gives the strongest measured operating point, while higher fanout settings increase candidate coverage and emitted-pair volume. The generator run completes all 20 answers without fallback and keeps answer evidence tied to exported contract identifiers. Together, these results give the demo two kinds of evidence: runtime behavior at stream scale and end-to-end behavior at human-inspectable scale.

After the main walkthrough, the presenter can switch threshold, window size, input order, and clustered fanout. These variants make the demo interactive in a systems sense: the same controls that define the experiment also reshape evidence density, retention horizon, contract timing, and the amount of runtime work visible in the operator view.

\subsection{What the Evidence Shows}

The prepared evidence supports three claims. First, the live path has enough data to show real behavior: reports from multiple source classes produce incident families, cross-source clusters, and both correlation and novelty contracts. Second, the runtime boundary remains measurable on a larger public stream: the same cached embeddings and runtime API process thousands of records and expose throughput, latency, and emitted pairs. Third, the contract handoff remains inspectable when generation is enabled: answer coverage, fallback count, faithfulness, and LLM latency are reported next to the runtime state. A demo attendee does not need to infer behavior from a hidden log; the transition is visible through retained records, match pairs, clusters, exported contracts, and the final generated answer.

\section{Discussion and Conclusion}

\system{} is a demonstration of a system boundary: semantic-window state remains inside a persistent vector runtime, while the application receives bounded contracts that are stable enough to inspect, log, route, and summarize. The key point is not that vector search can find similar records; the key point is that streaming vector evidence can become observable application behavior. The demo therefore emphasizes the handoff from event stream to runtime evidence to contract output.

There are two takeaways for attendees. The first is operational: a semantic window should expose enough state for a human to understand why an application-level decision changed. In \system{}, the presenter can point from a contract back to retained events and join pairs, which makes the system easier to debug than a pipeline that only displays generated text. The second takeaway is architectural: keeping the join state inside a long-lived runtime avoids rebuilding semantic context at every application step. This is what lets the demo show freshness, boundedness, and contract emission as one continuous behavior.

The demonstration emphasizes a reusable systems pattern: evidence remains stateful and measurable, while the model-facing layer receives a compact, auditable contract instead of an open-ended context dump. The console therefore works as both a live systems walkthrough and an operational debugging workflow. A presenter can explain why an answer changed by pointing to configuration, retained records, emitted matches, and a contract field.

The prototype focuses on vulnerability intelligence because the domain makes semantic grouping and escalation easy to understand. The same pattern applies to other streaming AI tasks where new evidence must be incorporated during a long-running workflow: customer-support incidents, scientific-monitoring feeds, or operational telemetry. The live demo exposes the essential systems question: how can an AI pipeline keep semantic context fresh while preserving the runtime state that justifies its actions?

\bibliographystyle{ACM-Reference-Format}
\bibliography{References}

\end{document}
