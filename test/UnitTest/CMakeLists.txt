# UnitTest CMakeLists.txt
# 注意：这个文件主要作为占位符，实际的测试构建配置在上级目录的CMakeLists.txt中

# 由于使用了现代CMake方式，单元测试的构建配置已经在 test/CMakeLists.txt 中统一管理
# 这样可以避免重复配置和依赖管理问题

# 如果需要添加新的单元测试，请在 test/CMakeLists.txt 的 UNIT_TESTS 列表中添加